let chave = 'inscrição';
localStorage.setItem(chave, 'valor');

let minhaInscrição = localStorage.getItem(chave);

localStorage.setItem(chave, 'novo valor');

localStorage.removeItem(chave);

localStorage.clear();

function soma(){    
    let cafe = quantidade
}